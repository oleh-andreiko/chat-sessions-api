# Test-only dependencies. Kept apart from requirements.txt so the runtime
# dependency list stays exactly what the service needs to run.

-r requirements.txt
pytest==9.1.1
# Needed by FastAPI's TestClient to drive the app in-process.
httpx==0.28.1
