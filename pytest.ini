[pytest]
# The project root has to be importable for "from app... import", and pytest
# only adds the directory holding conftest.py — which is tests/, not the root.
# Without this, pytest works from the project directory and fails from anywhere
# else, which is a confusing way to greet someone who just cloned the repo.
pythonpath = .
testpaths = tests
